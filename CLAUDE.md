<div dir="rtl" style="text-align: right;">

# Bosta Return / Exchange Exporter (`Bosta-Return-Exchange-Exporter`)

![version](https://img.shields.io/badge/version-v6.0.0-blue)
![status](https://img.shields.io/badge/الحالة-قيد_التصفية-orange)

> # 🔴 الأداة دي اتدمجت — التطوير بقى في مكان تاني
>
> **كل اللي في الريبو ده اتنقل لـ [`Bosta-Orders-Upload`](https://github.com/ecommoda-dev/Bosta-Orders-Upload) v2.0.0 (13-09-2026).**
> الأداة هناك بقت بترفع التلات أنواع: شحن عادي (10) واسترجاع (25) واستبدال (30)،
> من نفس الشاشة بمبدّل نوع الرفع. التكليف والجرد في `MERGE-BRIEF.md` هناك.
>
> **🔴 أي تعديل على المطابقة أو عقد الشحنة أو مكنة S2 يتعمل هناك، مش هنا.**
> محرك مطابقة العنوان بقى **نسخة واحدة** في الأداة المدموجة — وده كان بند
> مفتوح في الريبوين. وقت النقل طلع إن النسختين كانوا **افترقوا فعلًا**:
> النسخة اللي هنا كان فيها إصلاحين مش موجودين هناك (`findLocalZones(null)`
> بترمي · ومدينة مش في الكتالوج الحي بتتدهور لـ«مفيش مطابقة» بدل ما توقف).
> الاتنين اتنقلوا للأداة المدموجة.
>
> ### ليه الريبو لسه هنا؟
>
> **متمسحش حاجة قبل ما الجديدة تشتغل حي وتستقر** (MERGE-BRIEF §٨):
>
> ```
> ① ✅ الدمج اتعمل — 13-09-2026
> ② ⬜ تشغيل مراقَب على الجديدة: صف استرجاع واحد + صف استبدال واحد،
>       يتراجعوا على داشبورد بوسطة        ← التالي
> ③ ⬜ أسبوع تشغيل فعلي بالتوازي — **والأداة دي تفضل شغّالة زي ما هي**
> ④ ⬜ الصفحة دي تتحول لصفحة تحويل على الجديدة — **متتمسحش**
>       🔴 الموظفين حافظين الرابط. مسحه = صفحة ميتة بلا رسالة
> ⑤ ⬜ الـ Worker يتساب شغّال **شهر** (رخيص) — أي نداء متأخر يلاقي رد
> ⑥ ⬜ بعد الشهر: الـ Worker يتشال، والريبو **يتأرشف مش يتمسح** — فيه تاريخ ٤ شهور
> ```
>
> ⚠️ **صفوف D1 بتاعة الأداة دي بتفضل.** قيمة `tool = 'bosta_exchange_export'`
> (٥٦٦ صف من 05-05-2026) **ما اتغيّرتش ولا اتهاجرت**: الأداة المدموجة بتكتب
> صفوف الاسترجاع/الاستبدال تحت نفس القيمة، وتاب السجل بتاعها بيقرا القيمتين
> مع بعض. ده اختيار «أ» في MERGE-BRIEF §٥، **وقرار أحمد إنه يتأجّل لحد ما
> التجربة تكتمل**. أي تغيير فيه بعدين لازم يمر بمهارة `ecommoda-tool-rename`.
>
> 🔴 **باقي قبل الأرشفة:** `skillsupdates20260910.md` (في الريبو ده) لسه
> ما اتطبّقش — خمس مهارات فيها بنود من الفحص الحي، منها ٦ بنود 🔴. الملف
> مكتفي بذاته وفيه Checklist تطبيق. **يتطبّق أو يتنقل قبل خطوة ⑥.**

---

**بتعمل إيه:** فحص أوردرات الاسترجاع/الاستبدال الجاهزة لبوسطة، و**رفعها على بوسطة مباشرةً بالـ API** (v6.0.0) — بتنشئ الشحنة وبتحدّث S2 (`custom.status_2_r_e`) في نفس النداء. تصدير Excel لسه موجود كخطة بديلة.
**مين بيستخدمها:** المخزن
**الإصدار:** Worker `v6.0.0` · الواجهة `v6.0.0`

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Bosta-Return-Exchange-Exporter/
الـ Worker : https://bosta-return-exchange-exporter-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: bosta-return-exchange-exporter-worker     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `check_employee` / `register_pin` / `verify_employee` / `log_logout` / `get_employees` | تسجيل الدخول |
| `fetch_candidates` | يجيب أوردرات الاسترجاع/الاستبدال المرشّحة من شوبيفاي (`status_2_r_e` + `courier = Bosta`) — بيرجّع `currentCycle` (الدورة المفتوحة **بس**) + `cycleInfo` + `outgoingItems` لكل أوردر، ومابيرجّعش `returns[]` ولا `lineItems[]` خالص (v5.2.0 · v5.5.0) |
| `check_export_duplicates` | يفحص لو **نفس دورة** الأوردر دي اتصدّرت قبل كده — المفتاح = اسم الأوردر + `cycleName` (v5.3.0) |
| `record_export` | يسجّل عملية تصدير Excel (بيتمنع لو فيه تكرار من غير `allowRepeat`) — بيخزّن `cycleName`/`cycleCreatedAt` في `extra` (v5.3.0) |
| `confirm_upload` | بيفحص دورات الاسترجاع من شوبيفاي الأول ويرفض بـ `409 CYCLE_BLOCKED` قبل أي كتابة (v5.2.0) — والرفض بيتسجّل في D1 كـ `cycle_block` مع `logged` في الرد (v5.4.0). بعدين يكتب S2 الجديد (`In-Return` للاسترجاع · `Ready` للاستبدال) + وقت التحديث، ويتحقق مباشرة من شوبيفاي |
| `record_manual_confirmation` | **غير مستخدم من الواجهة الحالية** (v4.0.0 دمجت خطوتين الاستبدال في `confirm_upload`) — الكود سايبه موجود لو احتاجوه تاني |
| `get_logs` / `get_logs_count` / `get_logs_export` | سجل العمليات — فلاتر multi-select (`employees`/`types` CSV) + `search`، موحّدة بين التلاتة عبر `buildLogFilterSQL`. `get_logs_export` بيرجّع `cap`/`total`/`truncated` كمان (v5.0.0) |
| `get_districts` | **v6.0.0** — من غير `cityId`: كل مدن بوسطة · مع `cityId`: مناطقها (`dropOffAvailability === true` فقط) |
| `upload_re` | **v6.0.0** — 🔴 **بينشئ شحنات حقيقية بفلوس.** حارس الدورات → قراءة الأوردرات من شوبيفاي من جديد → إنشاء الشحنة (`type 25`/`30`) → كتابة S2 + التحقق → السجل. سقف الدفعة ٢٥ |
| `cancel_re` | **v6.0.0** — إلغاء شحنة بالـ `trackingNumber`. الإلغاء **بيحرّر** الـ `uniqueBusinessReference` فإعادة الرفع بعد التصحيح شغّالة. **مابيرجّعش S2** |
| `diag` | فحص ذاتي بدون كتابة (شوبيفاي · D1 · **بوسطة: المفتاح + عدد المدن والمناطق** · Origin) — مفيش قيم أسرار في الرد |
| `get_config` | رقم نسخة الـ Worker — تستخدمه الواجهة لحارس نسخة الـ Worker (v4.1.0) |

## D1

```
tool  : bosta_exchange_export
type  : scan · export_return · export_exchange · confirm_return · confirm_exchange ·
        manual_confirm_return · manual_confirm_exchange · login · cycle_block ·
        upload_re_return · upload_re_exchange · re_upload_failed ·
        re_shopify_write_failed · re_cancelled          ← الخمسة دول v6.0.0
```

> كل `confirm_upload` ناجح بيكتب **كمان** صف في `tool = 'metafields_change'` (`type = 'update'`)
> — ده مطلوب عشان الـ cycle-time / R-E-cycle KPIs (بتتقرا من `metafields_change` بس) تشوف التحديث ده.

> 🔴 **`cycle_block` (v5.4.0) + الخمس قيم بتوع v6.0.0 لسه محتاجين تسجيل في
> `ecommoda-constants` §7.** القاعدة (`worker-builder` Rule 7) بتقول التسجيل
> **قبل** أول `writeLog` مش بعده — ده استثناء واعي، متكتب هنا عشان اللي هيقرا
> بعد كده مايفتكرش إن التسجيل اتعمل.

⚠️ **`re_upload_failed` منفصلة عمدًا عن `re_shopify_write_failed`.** الأولى =
الشحنة **ما اتعملتش أصلًا**، فإعادة المحاولة آمنة. التانية = الشحنة **موجودة
فعلًا** عند بوسطة ومعاها رقم تتبع، والكتابة الرجعية على شوبيفاي هي اللي فشلت.
خلطهم بيخلي أي إعادة محاولة تعمل **شحنة مكررة بفلوس حقيقية**، مش مجرد صف سجل
ناقص. (نفس الفصل الموجود في `Bosta-Orders-Upload`.)

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
           CATALOG_KV (اختياري — من غيره كاش الكتالوج بيشتغل على Cache API تلقائيًا)
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY   ← الأخير جديد في v6.0.0
Vars     : SHOP_DOMAIN   ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي — لسه ما اتضيّقتش)
```

> `BOSTA_API_KEY` **يتشارك** مع باقي أدوات بوسطة — EcomModa عندها حساب بوسطة
> واحد (`ecommoda-constants` §3). مفيش `_1ry`/`_2ry` هنا — دول بتوع Khiam Store.

⚠️ بعد إضافة السر: Deployments → Version History → ⋯ → **Promote version**.
من غيرها القيمة بتفضل `undefined` و`?action=diag` هيقول `keySet: false` وزرار
الرفع هيفضل مقفول (والتصدير Excel هيفضل شغّال عادي).

## CORS

`ALLOWED_ORIGINS` صارمة — لأن الأداة **كتابة**: بتعمل `metafieldsSet` على
أوردرات حية (S2)، **وبقت من v6.0.0 بتنشئ شحنات بفلوس حقيقية**.

## خط الأساس بعد النقل

> مفيش خط أساس مباشر من أحمد وقت النقل. البديل — آخر صف `scan` ناجح في D1
> **قبل** النقل (23-08-2026، 08:25 UTC): فحص استرجاع رجّع **أوردر واحد مطابق**.
> استخدم نفس نوع الفحص (استرجاع) بعد النقل للمقارنة. بند مفتوح بوعي — راجع «مسائل مفتوحة».

## الاختبارات

```
node tests/re-payload.test.cjs        # عقد إنشاء الشحنة — الاتجاه · الـ cod · المرجع الفريد
node tests/upload-flow.test.cjs       # مسار الرفع كامل على fetch مزيّف — 3003 · 11000 · 500 · الترتيب
node tests/address-matching.test.cjs  # ترجيح مطابقة المنطقة — نسخة من اختبار Bosta-Orders-Upload
```

كلها بتشتغل بـ Node عادي من غير أي تنصيب، وبتحمّل `index.js` في sandbox.

🔴 **`address-matching` نسخة مقصودة** من نفس الاختبار في `Bosta-Orders-Upload`،
لأن محرك المطابقة نفسه اتنسخ هنا. نسخة كود من غير نسخة اختبارها = النسختين
بيفترقوا في صمت. أي تعديل في الترجيح يتعمل في الاتنين.

## فخاخ الأداة دي

### فخاخ الرفع المباشر (v6.0.0)

- 🔴 **الاتجاه بيتقلب حسب النوع، والغلط فيه بيرجّع 500 مش 400.**
  في الاسترجاع (`type 25`) عنوان العميل بيروح `pickupAddress`، وفي الاستبدال
  (`type 30`) بيروح `dropOffAddress`. بوسطة بتملا ناحية المخزن لوحدها من
  `businessLocationId`. عنوان العميل في `dropOffAddress` على CRP بيرجّع
  `500 · Cannot read properties of undefined (reading 'city')` — **مش خطأ
  واضح بكود**، فـ`humanizeBostaError` لازم تتعامل مع 500 بلا `errorCode`.
- 🔴 **`Math.abs` على الـ `cod` كارثة هنا.** `Bosta-Orders-Upload` بتعملها وهي
  صح لـ S1 (سالب هناك = العميل دفع زيادة). هنا السالب معناه **بوسطة بتدفع
  للعميل** — من ٤ أوردرات R/E مقيسة، **٣ سالبين**. النسخ الحرفي بيحوّل
  «رجّعله ٢٠٠٠» لـ«حصّل منه ٢٠٠٠».
- 🔴 **سقف الاسترداد `-2000` حد من بوسطة** (`errorCode 3008`)، مش قرار داخلي.
  الأداة بتقص عنده **وبتعلن القص على الصف** — القص الصامت بتاع الإكسيل معناه
  إن العميل بياخد أقل من حقه ومحدش شايف. (`#53517`: مستحق 2700 · الملف 2000 ·
  وملاحظة الأوردر مكتوب فيها «متبقي 700ج».)
- 🔴 **`uniqueBusinessReference` حاجز حقيقي، و`terminate` بيحرره.** مقيس حيًا:
  نفس القيمة وشحنتها عايشة → `400 · 11000` · بعد الإلغاء → `201` · نفس
  `businessReference` بقيمة فريدة مختلفة → `201` · ونفس القيمة على **نوع شحنة
  مختلف** → `11000` (الفرادة على الحساب كله). عشان كده القيمة هنا
  `#12345-R{n}` / `#12345-EX{n}` من اسم الدورة — **`12345` لوحده ممنوع**، ده
  اللي شحنة S1 بتبعته وهيصطدم.
  و`businessReference` بيفضل `order.name` بالحرف: كل سكانرات الستاك بتدوّر بيه.
- ⚠️ **الأوردر بقى ليه أكتر من شحنة عند بوسطة تحت نفس الـ `businessReference`**
  (شحنة S1 + شحنة R/E). أي أداة بتعمل `search` وتاخد `[0]` بقت بتقرا شحنة
  عشوائية. **الحالة دي قايمة من الإكسيل أصلًا** — الـ API مش بيخلقها، بس
  بيخلينا مسؤولين عنها. الفلترة تبقى على `type.code` (10/25/30) مش على النص.
- 🔴 **«تم جزئيًا» (أصفر) مش فشل.** معناها الشحنة **موجودة عند بوسطة** ومعاها
  رقم تتبع، وحاجة بعدها ما تمّتش (كتابة S2 مثلًا). إعادة الرفع بتعمل شحنة
  تانية بفلوس. الصف بيقول للموظف يصلّح باليد أو يلغي الشحنة ويرفع تاني.
- ⚠️ **`flexShippingInfo` مابيتبعتش.** بوسطة بتحطها لوحدها
  (`{isOrderEligible:true, amountToBeCollected:100}`) وبتعلّمها
  `status: "Not Applicable"` على شحنات R/E — فبعتها مابيغيّرش حاجة.
- ⚠️ **`goodsInfo.amount` عليه تأمين ١٪ تلقائي** عند بوسطة (2600 → 26). المهارة
  `bosta-api-helper` 8.9 بتقول «مالوش أثر مالي تلقائي» — **ده غلط**، اتصحّح
  بقياس حي.
- ⚠️ **`allowToOpenPackage: true` على شحنات الاسترجاع كمان** — مطابق للإكسيل،
  بس **لسه مش معروف** هل رسم الـ 7ج بيتحسب على CRP أصلًا (بند ب-٨ مفتوح).
  متتشالش كـ«تنضيف كود» ومتتغيّرش من غير قرار.
- 🔴 **محرك مطابقة العنوان نسخة حرفية** من `Bosta-Orders-Upload` v1.3.0
  (`§BOSTA::catalog` → `§BOSTA::resolveAddress`). اتنسخ **ومعاه اختباره**.
  أي تعديل في الترجيح يتعمل في الاتنين — أو في البلوك المشترك المقترح في
  `bosta-api-helper`.
- 🟠 **صفوف «المدينة مشكوك فيها» مستثناة من «تحديد الكل»** زيها زي الدورات
  المتوقّفة. المنطقة الناقصة fallback محايد (بتدخل مسار العناوين غير الواضحة
  عند بوسطة)؛ **المدينة الغلط مش محايدة** — بتحدد الفرع والتسعيرة.

### فخاخ سابقة (لسه سارية)

- **`In-Return` بتتكتب يدوي مش من مزامنة بوسطة تلقائية.** استثناء مقصود
  اتأكد مع أحمد 06-08-2026 (موثّق في رأس `index.js`) — الأصل حسب
  `ecommoda-order-lifecycle` إن `In-Return` "بوسطة بس". هنا لسه مفيش
  auto-sync حي من بوسطة لشوبيفاي، فالتأكيد اليدوي هو المُحفّز الوحيد.
  يتراجع لو/لما الـ sync ده يتعمل.
- **`checklist` في `confirm_upload` audit-only.** مش بتمنع الكتابة سيرفر-سايد
  — البوابة الحقيقية هي مودال الواجهة، مش الـ Worker. (⚠️ ده **مابينطبقش**
  على حارس الدورات المضاف في v5.2.0 — ده بيتفحص سيرفر-سايد فعليًا من شوبيفاي
  وبيرفض قبل الكتابة.)
- 🔴 **`exchangeLineItems` الفاضية مش معناها استرجاع.** تعديل أوردر بيشيل
  قطعة الاستبدال اللي شوبيفاي عملها ويحط واحدة بالإيد بيفضّي الـ connection
  **نهائيًا** على استبدال حي — وشوبيفاي في الـ Admin لسه بتطبع «Exchange item
  for return #X» على السطر المشال، يعني الشاشة والـ API بيتناقضوا. مقيس حيًا
  على `#53531` و`#53701` (09-09-2026): الملف طلع من غير `Package Description`
  ولا `No. of Items`، و`Goods Value` رجع لسعر القطعة **الراجعة**
  (`#53701`: 2600 بدل 2400 — غلط في الفلوس راح للكوريير).
  القطع الخارجة بقت تتحسب في `§SHOPIFY::outgoingItems` — الدورة أولاً، وبعدين
  سطور الأوردر اللي `currentQuantity > 0 && unfulfilledQuantity > 0`.
  **fallback مش merge** (الدمج بيعدّ القطعة مرتين على استبدال سليم)، و**مابيعيدش
  التصنيف** (Rule 8 لسه بتتقرا من الدورة لوحدها). القاعدة →
  `ecommoda-order-lifecycle` Rule 8، والتحديث المقترح في
  `skillsupdates20260909.md`.
- **الأوردر ممكن يكون عدّى بأكتر من دورة استرجاع/استبدال، والـ S2 بيقول على
  الأخيرة بس.** أي حساب هنا (وصف الشحنة · العدد · `Goods Value`) لازم يتبني
  من `order.currentCycle` — الدورة المفتوحة الوحيدة — مش من `returns[]` كلها.
  ده كان **الباج الأساسي لحد v5.2.0**، وقيس حيًا على `#51656`: تلات دورات
  (R1 مقفولة · R2 مقفولة · R3 مفتوحة) طلّعت `Return #Items = 3` و
  `Goods Value = 6300` بدل قطعة واحدة و`1750` — يعني قطع رجعت المخزن خلاص
  اتشحنت تاني في ملف بوسطة. القاعدة → `ecommoda-order-lifecycle` Rule 15 ②.
  الحماية الهيكلية: الـ Worker مابيرجّعش `returns` للواجهة أصلاً، فالتجميع
  القديم بقى **مستحيل** من الفرونت إند.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
آخر نسخة مرقّمة كانت 3.33.html، اتمسحت في commit 9695f10 (بعد ما اتسمّت Index.html).
git show 91c6027~1:3.33.html
```

## بصمة المهارات

> الصيغة والقواعد والمهارات اللي بتدخل الجدول → `ecommoda-skill-versioning`
> Step 4. مهارة مالهاش رقم إصدار مابتدخلش الجدول.

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v3.7.0 |
| ecommoda-html-builder | v6.2.0 |
| ecommoda-order-lifecycle | v1.2.0 |
| ecommoda-constants | v3.1.0 |

آخر مطابقة: 22-09-2026 · `index.js` v6.0.1 · `index.html` v5.5.0
🔴 البصمة دي **ما اتحدّثتش** في v5.5.0/v5.6.0 ولا في v6.0.0 — راجع «مسائل مفتوحة».
v6.0.0 اتكتب على `bosta-api-helper` v1.1.0 (Step 8 كاملة) و`worker-builder`
v3.0.0 **للبنود اللي لمسها بس** (5A ④ · ⑩ · ⑪ · ⑫ · ⑬ · ⑭) — مش مراجعة كاملة.
v6.0.1 (22-09-2026): استبدال `check-log-values.mjs` بالنسخة المصلَّحة (كانت
بتفوّت `{ tool, type }` shorthand في صمت) + تنفيذ الحارس الديناميكي (الطبقة ٥ ·
`worker-builder` Step 7-ج) في `writeLog`/`writeLogsBatch` — `LOG_REGISTRY`
مبني من `log-values.json` بعد تسجيل ١٠ قيم كانت ديناميكية غير مسجّلة
(`upload_re_*` · `re_upload_failed` · `re_shopify_write_failed` ·
`export_*` · `confirm_*` · `manual_confirm_*`) + تصحيح `update` ليتسجّل تحت
`tool: 'metafields_change'` (الصف الحقيقي اللي بيتكتب، مش تحت اسم الأداة).
مراقبة بس — صفر تعديل على منطق تشغيلي.
🔴 معلّقة: — `cycle_block` + الخمس قيم بتوع v6.0.0 (+ الست قيم الإضافية اللي
اتسجّلت في v6.0.1) لسه مستنيين التسجيل في `ecommoda-constants` §7 (الجدول
هناك اتنقل لـ `log-values.json` بتاع كل ريبو من 21-09-2026 — التسجيل دلوقتي
هنا في الملف نفسه، وده اللي حصل في هذا التسليم).
البصمة لسه متسابة عن قصد بدل ما تدّعي مراجعة كاملة ما حصلتش — محتاجة
`skills-sweep`.

## مسائل مفتوحة

- ✅ **رفع الاسترجاع/الاستبدال بالـ API — اتعمل 10-09-2026 (v6.0.0).** العقد
  كله (`type 25`/`30`) اتقاس حيًا على حساب EcomModa والتفاصيل في
  `PHASE2-LIVE-CHECKS.md` §٢ وفي «فخاخ الرفع المباشر» فوق. **الإكسيل اتساب
  موجود** كخطة بديلة: عقد بوسطة يتغيّر · المفتاح مش متاح · أوردر الـ API
  رافضه.
  🔴 **باقي قبل أول تشغيل حي:**
  1. تسجيل الخمس قيم `type` في `ecommoda-constants` §7.
  2. `BOSTA_API_KEY` يتضاف كسر + **Promote**.
  3. `Bosta-Orders-Upload` تتشغّل حيًا الأول — المرحلة دي قايمة على محركها وهو
     **لسه ما اشتغلش ولا مرة**. لو فيه باج في المطابقة، اكتشافه على شحنة عادية
     أرخص بكتير من مندوب رايح يستلم قطعة من فرع غلط.

- ⏸️ **رقم التتبع مش بيتكتب في أي ميتافيلد — قرار أحمد المؤجَّل (ق-٥).**
  `custom.bosta_tracking_number` واحد ونوعه `number_integer` وبتاع شحنة S1،
  ومفيش مقابل لـ S2. لحد ما يتعمل `custom.bosta_tracking_number_s2`، الرقم
  عايش في سجل D1 وعند بوسطة تحت `businessReference` بس.
  ⚠️ النتيجة العملية: الربط من شوبيفاي لشحنة الـ R/E بيمرّ بالسجل أو بالبحث في
  بوسطة، مش بحقل على الأوردر.

- 🟡 **ب-٨ — `allowToOpenPackage` على CRP.** هل رسم الـ 7ج بيتحسب على شحنة
  استرجاع أصلًا؟ يتراجع من فاتورة بوسطة. لحد القرار: `true` زي الإكسيل.

- 🟡 **ب-١٢ — طباعة البوليصة بعد الرفع بالـ API.** الموظف كان بيطبع من داشبورد
  بوسطة بعد رفع الملف؛ دلوقتي الشحنة بتتعمل من غير ما حد يفتح الداشبورد.
  إما يفضل يطبع من هناك، أو `mass-awb` (أداة تانية — `bosta-awb-api.md`).

- 🟡 **ب-٥ — جرد الشحنتين على الستاك.** الأوردر بقى ليه شحنتين تحت نفس
  `businessReference`. `bosta-order-lookup-worker` و`order-printer-worker`
  محتاجين مراجعة: بياخدوا `[0]`؟ الفلترة على `type.code` مش النص.

- 🟡 **`ecommoda24` — الويبهوك القديم.** لسه مش معروف حي ولا ميت. مقيس
  10-09-2026: `bosta_order_type` و`bosta_last_sync_time` **فاضيين على آخر ١٠
  أوردرات `Shipped`** — قرينة قوية إنه مش بيكتب، مش إثبات. مش حاجز على البناء،
  بس يتعرف قبل التشغيل الحي.

- 🔴 **`skillsupdates20260910.md` — تصحيحات المهارات لسه ما اتطبقتش.** خمس
  مهارات فيها بنود من الفحص الحي، منها ٦ بنود 🔴: قيم `type.value` الحقيقية ·
  قاعدة `uniqueBusinessReference` · عقد CRP/Exchange (Step 8b جديدة) ·
  تسجيلات §7 · وفلتر `metafields.custom.KEY:*` اللي بيتجاهل نفسه بصمت.
  الملف مكتفي بذاته وفيه Checklist تطبيق. **البصمة فوق متسابة زي ما هي لحد ما
  ده يخلص** — تحديثها قبله معناه ادّعاء مراجعة ما حصلتش.
  (والملف السابق `skillsupdates20260909.md` **اتطبّق بالكامل** — order-lifecycle
  بقى v1.5.0 وgraphql-helper v2.1.0 — فمفيش بند متبقي منه.)

- 🟠 **حالتين شك كاذب في كاشف المدينة** اتقاسوا على عناوين مرتجعات حقيقية:
  `#53075` (الدقهلية + «الجماليه») و`#52888` (الإسكندرية + «Green Plaza»).
  الاتنين بيولّعوا 🟠 على صفوف سليمة. مش كاسر (الموظف بيراجع ويحدد) بس بيزوّد
  الشغل اليدوي — يستاهلوا يبقوا حالتين في `tests/address-matching.test.cjs`
  في **الريبوين**.


- ✅ **قطع الاستبدال بعد تعديل الأوردر — اتصلحت 09-09-2026 (Worker v5.5.0 ·
  الواجهة v5.6.0).** التفاصيل والقياس في «فخاخ الأداة دي» فوق. اللي اتعمل:
  بلوك `§SHOPIFY::outgoingItems` جديد في الـ Worker
  (`itemsFromCycle` / `itemsFromUnfulfilledLines` / `resolveOutgoingItems`)،
  و`fetch_candidates` بقى يرجّع `outgoingItems` محسوبة سيرفر-سايد — الواجهة
  مابقتش تشوف `exchangeLineItems` ولا `lineItems` خام، فالتجميع الغلط بقى
  **مستحيل** منها (نفس حماية `returns` من v5.2.0). كود تحذير جديد
  `EXCHANGE_ITEMS_RECOVERED` (الصف بيتصدّر + الموظف بيتنبّه)، و
  `EXCHANGE_WITHOUT_ITEMS` بقى **حاجب** بدل تحذير بقرار أحمد — صف بوسطة من غير
  ولا قطعة خارجة مش شحنة أصلاً. الحجب متطبّق سيرفر-سايد في `confirm_upload`
  كمان، فحارس الدورات مابقاش استعلام scalars بس (batch 50 → 20 + نفس
  halve-and-retry بتاع الـ query cost). `MIN_WORKER_VERSION` في الواجهة بقى
  `5.5.0` — Worker أقدم مابيرجّعش `outgoingItems` وكان هيفضّي الأعمدة بصمت.
  🔴 **باقي:** تطبيق `skillsupdates20260909.md` على المهارات (أحمد، جلسة منفصلة).

- 🔴 **بصمة المهارات في الريبو ده متأخرة — مافيش جرد اتعمل.** الجدول فوق بيقول
  `worker-builder v2.0.0` · `html-builder v6.2.0` · `order-lifecycle v1.2.0` ·
  `constants v1.4.3`، والموجود فعليًا وقت الكتابة:
  **worker-builder v3.0.0 · html-builder v7.0.0 · order-lifecycle v1.4.0 ·
  constants v2.0.0 · shopify-graphql-helper v2.0.0**. تعديل v5.5.0/v5.6.0
  راجع **Rules 8 / 13 / 14 / 15** من `order-lifecycle` بس — مافيش مراجعة
  امتثال كاملة، والبصمة **متسابة زي ما هي عن قصد** بدل ما تدّعي مراجعة ما
  حصلتش. محتاج `skills-sweep` على الأداة دي.

- ✅ **دورات الاسترجاع — اتصلحت 02-09-2026 (v5.2.0).** التفاصيل في «فخاخ
  الأداة دي» فوق. اللي اتعمل: الـ Worker بيرتّب `returns[]` بـ `createdAt`
  (مش بترتيب المصفوفة)، بيشيل `CANCELED`/`DECLINED` **قبل** الترتيب (الـ
  `closedAt` بتاعهم `null` وبيفبرك تداخل وهمي)، وبيرجّع `currentCycle` واحدة
  + `cycleInfo`. اتضاف حارس الدورة الواحدة: `CYCLE_OVERLAP_OPEN` ·
  `NO_OPEN_CYCLE` · `CYCLES_TRUNCATED` · `ORDER_NOT_READABLE` بيمنعوا التصدير
  وتحديث الحالة (واجهة + سيرفر-سايد في `confirm_upload`)، و`MULTI_CYCLE` ·
  `CYCLE_OVERLAP` · `TYPE_MISMATCH` · `EXCHANGE_WITHOUT_ITEMS` تحذير بس
  (flag it, never move it). عمود جديد «الدورة المفتوحة» + نافذة مراجعة في
  الواجهة.
- ✅ **«Delivery Notes» — اتقرر 02-09-2026 (v5.3.0): يتملى من `order.note`.**
  كان بيقرا الحقل من غير ما الـ query تطلبه، فكان **دايمًا فاضي** من أول يوم.
  ⚠️ الـ `note` ملاحظات **داخلية** لخدمة العملاء (على `#51656`: «أوردر استبدال
  / لا يوجد مصاريف شحن / أوردر استرجاع») — الكلام ده بقى بيوصل للمندوب فعليًا.
  `flattenNote()` بتحوّله لسطر واحد قبل الكتابة في الملف.
- ✅ **رفض `confirm_upload` بسبب الدورات — اتسجّل 02-09-2026 (v5.4.0).**
  قاعدة «reject + log» بقت مكتملة: صف D1 لكل أوردر متمنوع بـ
  `type = 'cycle_block'` شايل الكود والقيمة والإجراء والحالة اللي **ما
  اتكتبتش**، وفلتر جديد في تاب السجل. فشل الكتابة في D1 بيرجع `logged: false`
  والواجهة بتقوله صراحة (`worker-builder` Step 5A ⑦) — الرفض بيفضل قايم.
  🔴 **باقي:** تسجيل `cycle_block` في `ecommoda-constants` §7 (أحمد بيعمله).
- ✅ **مفتاح فحص التكرار — اتصلح 02-09-2026 (v5.3.0): اسم الأوردر + اسم
  الدورة.** كان بالاسم بس، فأوردر بدورة تانية **شرعية** (`MULTI_CYCLE`) كان
  بيتقفل كـ«مكرر» ويحتاج `allowRepeat` — والحماية كانت بتفقد معناها أوردر ورا
  التاني. `record_export` بقى بيخزّن `cycleName`/`cycleCreatedAt` في `extra`.
  **الصفوف القديمة** (قبل v5.3.0، مالهاش اسم دورة) لا بتتلغي ولا بتتحسب على
  عماها: بتتطابق مع الدورة الحالية **بس** لو `timestamp >= cycle.createdAt` —
  تصدير حصل قبل ما الدورة توجد مستحيل يكون تصدير ليها. مفيش migration.
- ✅ **`ALLOWED_ORIGINS` — راجعتها 02-09-2026: مفيش `ecommoda24.github.io` في الكود
  الحالي.** `ecommoda-constants` §5 و§11 بند 10 لسه بيحسبوا الأداة دي من الـ ٥
  اللي محتاجة تنظيف — القيمة الوحيدة الموجودة فعليًا هنا (`index.js`) هي
  `https://ecommoda-dev.github.io`. يظهر إن ده اتصلح قبل كده من غير ما يتسجّل،
  أو إن جرد `ecommoda-constants` عنده بيانات قديمة عن الأداة دي — يستاهل
  تصحيح هناك.
- ✅ **عمود «Brand Name» — اتشال 02-09-2026 (واجهة v5.5.0)، بطلب مباشر من أحمد.**
  كان آخر عمود في ملف بوسطة (العمود U) وبيتكتب فيه `Ecommoda` ثابت لكل صف.
  الملف بقى **20 عمود** (`Full Name` … `Pay With Bosta Credits?`) — ترتيب ومكان
  باقي الأعمدة ما اتغيّرش. تغيير واجهة بس؛ الـ Worker ما اتمسّش (`v5.4.0`).

- **خط الأساس بعد النقل معتمد على استنتاج من D1 مش رقم مؤكد من أحمد**
  — راجع "خط الأساس بعد النقل" فوق.
- **`record_manual_confirmation` endpoint حي بس غير مستخدم** من أي مكان
  في الواجهة الحالية (v4.0.0+). سايبه الكود كما هو — قرار موثّق في هيدر
  `index.js` نفسه، مش حاجة اتكشفت في النقل.
- ✅ **تحويل الواجهة الكامل لـ `ecommoda-html-builder` v6.0.0 — اتعمل 02-09-2026
  (v5.0.0).** التابات الرئيسية (`.main-tabs-bar`/`.main-tab-btn`)، قسم
  فلاتر/جدول موحّد (`unified-section`) لكل من "الأوردرات الجاهزة للملف"
  وسجل العمليات، كل الفلاتر multi-select (`msState.orderStatus` /
  `msState.logType`)، `--container-max` Tier M (1200px)، About/Changelog على
  شِل `.eco-modal`، زرار الإغلاق بإطار أحمر حقيقي (كان نص أحمر بس رغم اسم
  الكلاس الصح)، سجل العمليات جدول حقيقي بعمودي تاريخ/وقت منفصلين وترتيب على
  الأعمدة، وصيغة `📅 …/…/… 🕐 …:…`. الـ Worker اتحدّث بالتوازي (v5.0.0) —
  `get_logs*` بقت تاخد `employees`/`types` CSV بدل قيمة واحدة.
  🟡 **لسه ما اتعملش (مُستحسن، معروف ومتعمّد):** Freeze on Scroll
  (`tabs-and-modals.md` § 1b — هيدر مضغوط + chip تاب عند السكرول). مش كاسر
  لأن الأداة من غيره بتشتغل عادي، بس التابات هتفضل واخدة مساحة تابتة أثناء
  السكرول. موثّق في مودال About (تشريح الكود) كمان.
  ⚠️ **"النتائج" في تاب سجل العمليات = عدد كلي من السيرفر مش `filtered.length`**
  — استثناء متعمّد عن `data-table-standard.md` § 7 بسبب الـ pagination
  الحقيقي (Log Filter Model v2)؛ موثّق في مودال About.
- ✅ **تبسيط تبويب التصدير — اتعمل 02-09-2026 (v5.1.0)، بطلب مباشر من أحمد.**
  حذف صندوق الإحصائيات الخمسة (مطابق للشروط / محدد للتصدير / تم التصدير /
  تم تحديث الحالة؟ / الحالة التالية) وزراري "تنظيف" و"تحديث حالة الأوردر
  إلى ...". بقى فيه زرار تصدير واحد بس — كل الأوردرات محددة افتراضيًا
  (`fetchCandidates` بيملي `state.selectedOrderIds` بكل الـ IDs)، والزرار
  بيبدأ "📥 تصدير الكل" ويتحول لـ "📥 تصدير المحدد فقط (العدد)" لو الموظف
  شال تحديد أي أوردر (`updateExportBtnLabel`). بعد نجاح `performExcelExport`
  بيتفتحوا تلقائيًا: تاب جديد على `business.bosta.co/orders/upload/smart-upload`
  + نافذة تأكيد تحديث الحالة (`openStatusConfirmModal`) — من غير زرار وسيط.
  `clearResults()` لسه موجودة داخليًا (بتتنده عند تبديل نوع العملية/الدخول/
  الخروج) رغم حذف زرار "تنظيف" اللي كان بينده عليها.

آخر تحديث: 13-09-2026 (إشعار الدمج والتصفية)

</div>
