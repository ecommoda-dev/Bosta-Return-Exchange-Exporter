<div dir="rtl" style="text-align: right;">

# تحديثات المهارات — 10-09-2026

> **المصدر:** الفحص الحي لعقد إنشاء شحنات الاسترجاع/الاستبدال عند بوسطة
> (`PHASE2-LIVE-CHECKS.md` → النتائج) + بناء `Bosta-Return-Exchange-Exporter`
> v6.0.0 + استعلامات حية على شوبيفاي و D1.
> **الملف ده مكتفي بذاته** — الجلسة اللي هتطبّقه مش محتاجة تقرا الجلسة الأصلية.
> الصيغة والتصنيف والإجراء → `ecommoda-skill-versioning`.

---

## 0 · الاكتشاف — في أربع سطور

عقد إنشاء الشحنة عند بوسطة **مش عقد واحد بمتغيّر `type`**. شحنة الاسترجاع
(`25`) وشحنة الاستبدال (`30`) بيقلبوا **اتجاه العنوان**، وبيستخدموا حقل
**تاني خالص** لوصف الطرد الراجع، وبيقبلوا `cod` **سالب** بحد أدنى، والحقل اللي
كنا فاكرينه «حماية من التكرار» طلع **حاجز حقيقي على مستوى الحساب كله**.

`bosta-api-helper` Step 8 مكتوبة كلها على `type: 10`، فأي أداة تكتب على أساسها
شحنة استرجاع بتاخد **500 بلا كود خطأ** — مش رسالة مفهومة.

---

## 1 · الأدلة الحية (كلها متقاسة 10-09-2026 على حساب EcomModa الإنتاج)

### 1.1 · تشريح شحنات موجودة (قراءة بس — من الملفات اللي اترفعت بالإكسيل)

٩ أوردرات، كل واحد عليه **شحنتين بالظبط** تحت نفس `businessReference`
(شحنة `Send` من S1 + شحنة R/E)، والبحث بيرجّعهم **الأحدث أول**:

```
#53849  Exchange 2538077787  cod -100   ·  Send 2662761824
#53197  Exchange 8146914656  cod  100   ·  Send 1278415660
#51261  Exchange 5151172736  cod  100   ·  Send 3953593300
#53531  Exchange 1785908954  cod  100   ·  Send 4756996281
#52928  Exchange 2674547770  cod  100   ·  Send 6938987600
#53240  CRP      1994985983  cod -1600  ·  Send 8675646319
#53517  CRP      7701163641  cod -2000  ·  Send 7025548734
#53027  CRP      9924550697  cod -2000  ·  Send 5585912261
#52704  CRP      4382545861  cod -1099  ·  Send 2059149753
```

- شحنات الإكسيل: `creationSrc = NEW_MASS_UPLOAD` · **`uniqueBusinessReference = null`** على التسعة.
- `#53531` اتصدّر ٣ مرات في سجل D1 وعليه شحنة Exchange **واحدة** → التصدير ≠ الرفع.

### 1.2 · شحنات تجريبية — ٨ اتعملت، ٨ اتلغت بـ `terminate` (٢٠٠)

| الغرض | النتيجة |
|---|---|
| CRP بعنوان العميل في `dropOffAddress` | ❌ **HTTP 500** · `Cannot read properties of undefined (reading 'city')` · **مفيش كود خطأ** |
| CRP بعنوان العميل في `pickupAddress` | ✅ 201 |
| Exchange بعنوان العميل في `dropOffAddress` | ✅ 201 |
| CRP من غير `specs` خالص | ✅ 201 |
| `returnSpecs` على النوعين | ✅ رجعت في الـ GET كاملة (وبوسطة بتضيف `descriptionAr` لوحدها) |
| `cod: -500` على 25 و30 | ✅ 201 |
| `cod: -2700` | ❌ 400 · `errorCode "3008"` · *"The Refund COD amount should be less than or equal -2000 EGP"* · **مفيش شحنة اتعملت** |
| العقدين (`?apiVersion=1` والغير موثّق) على 25/30 | ✅ الاتنين شغّالين · و`3003` بييجي بنفس الشكل → الرجوع التلقائي شغّال |

### 1.3 · `uniqueBusinessReference` — أربع حالات مقيسة

| الحالة | رد بوسطة |
|---|---|
| نفس القيمة وشحنتها **لسه عايشة** | ❌ 400 · `errorCode "11000"` |
| نفس القيمة **بعد `terminate`** الأصلية | ✅ 201 — **الإلغاء بيحرّر القيمة** |
| نفس `businessReference` بقيمة فريدة **مختلفة** (أو بدونها) | ✅ 201 |
| نفس القيمة على **نوع شحنة مختلف** | ❌ 400 · `11000` — الفرادة **على الحساب كله** |

> 📌 ده بيقفل سؤال كان مفتوح: أحمد كان فاكر إن بوسطة بتقبل التكرار عادي. اللي
> بيقبل التكرار هو **`businessReference`**، مش `uniqueBusinessReference` —
> والحقلين اتنين مختلفين. الالتباس ده كان مصدره صياغة المهارة نفسها.

### 1.4 · حاجات ظهرت في الـ GET من غير ما نبعتها

```
flexShippingInfo : { isOrderEligible: true, amountToBeCollected: 100,
                     status: "Not Applicable" }   ← بوسطة بتحطها لوحدها على R/E
pricing.insuranceFee : 1% من goodsInfo.amount     ← 2600 → 26
specs (من الإكسيل)   : packageType "Small" · weight 1 · مفيش size متخزّن
shipmentFees         : CRP 94–108 · Exchange 106–140 (+ التأمين)
```

### 1.5 · مواقع الاستلام الأربعة على الحساب

```
GeZMkbD7o               كليه البنات - مصر الجديده                  ← المعتمد (S1 و S2)
gc0hrw1nd2              Original Buisness Location (El-Zaytoun)   غير مستخدم
-8mpfFep0               Masr Al Jadidah                           غير مستخدم
0WPZCELExwssv2vANs96U   Original business location (Heliopolis)   غير مستخدم
```

### 1.6 · من شوبيفاي (استعلامات حية)

- تعريفات ميتافيلد الأوردر: **٣٢ تعريف**، فيهم `custom.bosta_tracking_number`
  **واحد بس** نوعه `number_integer` — **مفيش مقابل لـ S2**.
- `firstName` و`provinceCode` **50/50** على أوردرات R/E · `lastName` فاضي على واحد.
- التليفون **تلات أشكال**: `01…` · `+201…` · **`+20 12 71043044` بمسافات** (`#53849`).
- 🔴 **الفلتر `metafields.custom.KEY:*` مابيفلترش** — رجّع كل الأوردرات زي ما هي.

### 1.7 · من D1 (`tool = 'bosta_exchange_export'`)

| الشهر | أوردرات اتصدّرت | أوردرات اتأكّدت |
|---|---|---|
| 06-2026 | 22 | **0** |
| 07-2026 | 8 | **0** |
| 09-2026 (٩ أيام) | 22 | 22 |

---

## 2 · `bosta-api-helper` — المقترح: **v1.1.0 → v2.0.0**

> MAJOR لأن بند 2.1 و2.2 معناهم إن كود اتكتب على المهارة الحالية **بقى غلط**.

### 2.1 · 🔴 كاسر — Step 2: قيم `type.value` الحقيقية

**الحالي (Step 2، سطر التعليق فوق الفلتر):**
```javascript
// type values from Bosta: 'Send', 'EXCHANGE', 'CASH_COLLECTION', 'Return', 'RTO', 'CUSTOMER_RETURN_PICKUP'
const sends = deliveries.filter(d => (d.type?.value || d.type) === 'Send');
```

**المقيس حيًا:** `type` أوبچكت `{ code, value }` وقيم `value` بالشكل ده:
```
{ code: 10, value: "Send" }
{ code: 25, value: "Customer Return Pickup" }     ← مش "CUSTOMER_RETURN_PICKUP"
{ code: 30, value: "Exchange" }                    ← مش "EXCHANGE"
```

**الأثر:** أي فلتر اتكتب على `=== 'EXCHANGE'` بيرجّع **صفر** بصمت. ده نفس
عيلة فخ `STATE_MAP` بالظبط.

**الصح المقترح — الفلترة على الكود مش على النص:**
```javascript
// 🔴 الكود رقم ثابت، والنص بيتغيّر شكله حسب الـ endpoint. فلتر بالكود.
const TYPE_SEND = 10, TYPE_CRP = 25, TYPE_EXCHANGE = 30;
const code = d.type?.code ?? null;
const sends = deliveries.filter(d => (d.type?.code ?? null) === TYPE_SEND);
```

> ⚠️ **قبل التطبيق — نداء واحد يأكد.** القراءة دي جت من
> `GET /deliveries/business/{tn}`. جدول Step 2 مكتوب عن
> **`POST /deliveries/search`**، ومش متأكدين إن الاتنين بيرجّعوا نفس الشكل —
> وبوسطة عندها سابقة في ده (قيم `type` في الويبهوك **تالتة ومختلفة**، Step 8a).
> اعمل `POST /deliveries/search` بـ `businessReference: "#53849"` واقرا
> `type` بالظبط قبل ما تكتب الجدول. **التوصية بالفلترة على `type.code` صح في
> الحالتين**، فهي تتكتب حتى لو الشكل طلع مختلف.
>
> **يخص:** أي أداة بتفلتر شحنات بوسطة بالنوع.
> **المكان:** Step 2 · Step 7 · Quick Pitfall Checklist.

### 2.2 · 🔴 كاسر — 8.3 وصف `uniqueBusinessReference` ناقص وبيضلّل

**الحالي:**
> `uniqueBusinessReference : String(orderNumber)` ← بدون هاش — حماية بوسطة من التكرار (`errorCode 11000`)

المشكلة مش إن الجملة غلط — المشكلة إنها **مش كافية للقرار**. مابتقولش:
الفرادة على إيه بالظبط، بتتحرر إمتى، ولا إن أداة تانية على نفس الأوردر هتصطدم.

**الصح — الأربع حالات المقيسة (جدول 1.3 فوق) + الاستنتاجات:**

1. الفرادة **على الحساب كله وعبر كل الأنواع** — مش لكل نوع شحنة.
2. **`terminate` بيحرر القيمة** → إعادة الرفع بعد تصحيح غلط شغّالة.
3. **ملف الإكسيل (`NEW_MASS_UPLOAD`) مابيبعتش الحقل ده خالص** (`null` على ٩ شحنات).
4. **بلجن شوبيفاي بيحط Shopify order ID الرقمي** (`7196…`) — عشان كده هو
   مابيصطدمش بأدواتنا.
5. 🔴 **النتيجة العملية:** أي أداة تانية بترفع شحنة على **نفس رقم الأوردر**
   لازم قيمة مختلفة. `String(orderNumber)` **محجوز لشحنة S1**.

**القاعدة المقترحة تتكتب صراحة:**
```javascript
businessReference       : order.name          // "#12345" — ثابت، كل السكانرات بتدوّر بيه
uniqueBusinessReference : `${order.name}-R${n}`   // استرجاع  (n من اسم دورة شوبيفاي)
                          `${order.name}-EX${n}`  // استبدال
// 🔴 ممنوع "12345" لوحده في أي أداة غير أداة رفع S1.
```

> **يخص:** أي أداة بترفع شحنة لأوردر ممكن يكون عليه شحنة قبل كده.
> **المكان:** Step 8.3 · Quick Pitfall Checklist.

### 2.3 · 🔴 كاسر — Step 8b جديدة: عقد CRP والاستبدال

Step 8 كلها مكتوبة على `type: 10`. القسم الجديد لازم يقول:

**① الاتجاه بيتقلب:**

| | CRP — `type: 25` | Exchange — `type: 30` |
|---|---|---|
| عنوان **العميل** | **`pickupAddress`** | **`dropOffAddress`** |
| عنوان المخزن | `dropOffAddress` — بوسطة بتملاه من `businessLocationId` | `pickupAddress` — نفس الكلام |
| `specs` | **مش مطلوب** (مجرَّب من غيره → 201) | القطع **الخارجة** |
| `returnSpecs` | القطع **الراجعة** | القطع **الراجعة** |

**② الغلط في الاتجاه بيرجّع 500 مش 400:**
```
HTTP 500 · "Cannot read properties of undefined (reading 'city')" · مفيش errorCode
```
🔴 فأي `humanizeBostaError` لازم يتعامل مع **500 بلا `errorCode`** — الأمثلة
الحالية في المهارة كلها بتفترض كود موجود.

**③ `returnSpecs` نفس شكل `specs`:**
```json
"returnSpecs": { "packageType": "Parcel", "size": "SMALL",
                 "packageDetails": { "itemsCount": 1, "description": "…" } }
```
🔴 **و201 مش إثبات إن الحقل اتقبل** — نفس فخ `cityName` (8.2). الإثبات الوحيد
إن الحقل يرجع في `GET /deliveries/business/{tn}`.

**④ `cod` بإشارته:**
- سالب = بوسطة بتدفع للعميل عند الباب. مقبول على 25 و30.
- 🔴 **الحد الأدنى `-2000`** — أقل من كده `400 · errorCode "3008"` **ومفيش شحنة بتتعمل**.
- 🔴 **`Math.abs()` ممنوعة في أي أداة R/E.** بتحوّل «رجّعله ٢٠٠٠» لـ«حصّل منه ٢٠٠٠».

**⑤ `flexShippingInfo` مابيتبعتش على R/E** — بوسطة بتحطها لوحدها وبتعلّمها
`status: "Not Applicable"`.

> **يخص:** أي أداة بترفع شحنة استرجاع أو استبدال.
> **المكان:** Step 8b جديدة + سطر في Quick Pitfall Checklist.

### 2.4 · 🟡 مُستحسن — 8.9: `goodsInfo.amount` **له** أثر مالي تلقائي

**الحالي:**
> `/pricing/insuranceFeeEstimate` **endpoint منفصل تمامًا** … **مش بيتحسب أوتوماتيك وقت إنشاء الشحنة**. يعني `goodsInfo.amount` مالوش أثر مالي تلقائي غير أساس التعويض

**المقيس:** الشحنة الراجعة من الـ GET فيها `pricing.insuranceFee` = **١٪ من
`goodsInfo.amount`** (2600 → 26)، من غير أي نداء منفصل.

**الصح:** الـ endpoint المنفصل بيقدّر الرسم **قبل** الإنشاء، لكن الرسم نفسه
**بيتحسب تلقائيًا** على الشحنة. `goodsInfo.amount` **له تكلفة مباشرة**.

**ليه 🟡 مش 🔴:** مفيش كود منشور بيبقى غلط — بس أي قرار اتاخد على أساس «الحقل
ده ببلاش» (زي حط القيمة الكاملة دايمًا) كان على معلومة غلط.

> **يخص:** أي أداة بتحدد `goodsInfo.amount` · وأي حساب تكلفة شحن.
> **المكان:** Step 8.9.

### 2.5 · 🟡 مُستحسن — 8.7: بعد `terminate` الشحنة **بتختفي**

**الحالي:** بيقول إن المسار بيرجّع 200 وخلاص.

**المقيس:** بعد الإلغاء، `GET /deliveries/business/{tn}` بيرجّع
**`400 Delivery not found.`** والبحث بالـ `businessReference` **مابيرجّعهاش**.
يعني مفيش `state` نهائي زي 48 — الشحنة بتتشال من النتايج خالص.

**الأثر:** أي أداة بتتحقق من الإلغاء بقراءة الحالة هتفسّر الـ 400 على إنه فشل.
التحقق الصح = **الاختفاء نفسه**.

> **يخص:** أي أداة بتلغي شحنة أو بتوفّق حالات مع بوسطة.

### 2.6 · 🟡 مُستحسن — جدول أكواد الأخطاء: `3008`

الكود ده مش في المهارة ولا في التوثيق الرسمي. يتضاف لقايمة «معروفة من التجربة
بس» جنب 3002 · 3003 · 3009 · 11000:

```
3008 = The Refund COD amount should be less than or equal -2000 EGP
```

### 2.7 · ⚪ تحريري — 8.8: تأكيد إضافي من الحقل

بوسطة بتخزّن `packageType: "Small"` · `weight: 1` · **ومفيش `size`** على
الشحنات اللي اتعملت من ملف الإكسيل — يعني التساهل غير الموثّق اللي المهارة
بتحذّر منه **حقيقي ومطبَّق فعلًا على الحساب**. التحذير الحالي صح، ده دليل ليه.

### 2.8 · 🟡 مُستحسن (مقترح هيكلي) — محرك مطابقة العنوان يبقى بلوك مشترك

دلوقتي فيه **نسختين حرفيتين** من ~٤٥٠ سطر (`§BOSTA::catalog` →
`§BOSTA::resolveAddress`) في `Bosta-Orders-Upload` و`Bosta-Return-Exchange-Exporter`.
النسختين اتنسخوا **ومعاهم اختباراتهم**، بس ده حل مؤقت.

**المقترح:** يترقّى لبلوك «انسخه حرفيًا» جوّه `bosta-api-helper` — نفس نمط
`§SHARED` في `ecommoda-worker-builder` بالظبط، وده نمط الستاك المعتمد للكود
المشترك (مفيش package sharing بين الـ Workers).

⚠️ **مش مجرد تنظيم:** المحرك ده بيقرر **الفرع والتسعيرة** لكل شحنة. نسختين
بيفترقوا معناه أداتين بيبعتوا شحنتين لنفس العنوان على مدينتين مختلفتين.

### 2.9 · بند CHANGELOG المقترح

```markdown
## v2.0.0 — 10-09-2026

> **٣ بنود 🔴 · ٤ 🟡 · ١ ⚪.** المصدر: فحص حي لعقد `type 25`/`30` على حساب
> EcomModa — `skillsupdates20260910.md`.
>
> **ليه MAJOR (وده مختلف عن سابقة v2.1.0 بتاعة graphql-helper):** هنا فيه
> **قيمة حرفية غلط مكتوبة في سنيبت المهارة** (`'EXCHANGE'`) وقاعدة بصيغة
> مطلقة بتوجّه أي أداة جديدة لقيمة بتصطدم (`String(orderNumber)`). أي كود
> اتكتب على المهارة زي ما هي **بقى غلط فعلًا**، مش «مش مستفيد».
> (قاعدة الحسم: غلط = MAJOR.)

🔴 كاسر — قيم `type.value` الحقيقية: `Send` · `Customer Return Pickup` ·
   `Exchange`. الفلترة تتعمل على `type.code` (10/25/30) مش على النص
   يخص: أي أداة بتفلتر شحنات بوسطة بالنوع
   المكان: Step 2 · Step 7 · Quick Pitfall Checklist
   ⚠️ محتاج نداء تأكيد واحد على /deliveries/search قبل التطبيق

🔴 كاسر — `uniqueBusinessReference`: الفرادة على الحساب كله وعبر الأنواع،
   و`terminate` بيحررها، و`String(orderNumber)` محجوز لشحنة S1
   يخص: أي أداة بترفع شحنة لأوردر ممكن يكون عليه شحنة قبل كده
   المكان: Step 8.3

🔴 كاسر — Step 8b جديدة: عقد CRP (25) والاستبدال (30) — الاتجاه بيتقلب،
   `returnSpecs`، الـ 500 بلا كود، و`cod` بإشارته بحد -2000
   يخص: أي أداة بترفع استرجاع أو استبدال
   المكان: Step 8b · Quick Pitfall Checklist

🟡 مُستحسن — `goodsInfo.amount` عليه تأمين 1% تلقائي (النص الحالي بيقول
   العكس)
   يخص: أي أداة بتحدد goodsInfo.amount أو بتحسب تكلفة شحن
   المكان: Step 8.9

🟡 مُستحسن — بعد `terminate` الشحنة بتختفي (GET → 400 Delivery not found)
   يخص: أي أداة بتلغي شحنة أو بتوفّق حالات
   المكان: Step 8.7

🟡 مُستحسن — `errorCode 3008` (حد الاسترداد) يتضاف لأكواد التجربة
   يخص: أي أداة بترفع شحنة بـ cod سالب

🟡 مُستحسن — محرك مطابقة العنوان يترقّى لبلوك «انسخه حرفيًا»
   يخص: Bosta-Orders-Upload · Bosta-Return-Exchange-Exporter

⚪ تحريري — 8.8: دليل حي إن بوسطة بتخزّن packageType "Small" فعلًا
   يخص: لا شيء في الكود المنشور
```

---

## 3 · `ecommoda-constants` — المقترح: **v2.0.0 → v2.1.0**

> MINOR: مفيش قاعدة قديمة بقت غلط لأداة موجودة. البند 3.2 🔴 بس نطاقه أدوات
> جديدة/تانية. لو شايف إن ده يستاهل MAJOR، القرار قرارك — التصنيف مكتوب صريح
> عشان تقدر تراجعه.

### 3.1 · 🟡 مُستحسن — §3: حدود الـ COD

الحدين دلوقتي متكتوبين حرفيًا جوّه كود أداتين، ومفيش مصدر واحد ليهم:

```
COD_MAX        = 30000     ← موثّق في api.yaml
COD_REFUND_MIN = -2000     ← errorCode 3008 · مقيس حيًا 10-09-2026
```

🔴 **دول حدين في اتجاهين مختلفين، مش حد واحد.** يتكتبوا مع بعض عشان محدش
يفتكر إن `Math.abs()` كافية.

### 3.2 · 🔴 كاسر (نطاق ضيق) — §3.3: قاعدة `uniqueBusinessReference`

**الحالي:**
> قاعدة ثابتة لازم تتطبّق في **أي** أداة بترفع لبوسطة — الحقلين مع بعض دايمًا

الجملة دي بصيغتها المطلقة بتوجّه أي أداة جديدة تبعت `String(orderNumber)` —
وده بيصطدم `11000` مع شحنة S1. **يخص:** أي أداة بترفع شحنة تانية على نفس
الأوردر. التفصيل الكامل في بند 2.2 فوق؛ هنا يتكتب الجزء الحرفي:

```
businessReference       : "#" + رقم الأوردر     ← ثابت لكل الأدوات
uniqueBusinessReference : رقم الأوردر            ← 🔴 محجوز لأداة رفع S1 بس
                          "#12345-R{n}"          ← أداة الاسترجاع
                          "#12345-EX{n}"         ← أداة الاستبدال
```

### 3.3 · 🟡 مُستحسن — §3.1: مواقع الاستلام الأربعة

المهارة دلوقتي بتقول «٤ مواقع، المستخدم واحد، **الباقي يتسجل هنا الأول قبل
الاستخدام**». القيم بقت معروفة — تتكتب (جدول 1.5 فوق) عشان البند ده يتقفل.

### 3.4 · 🟡 مُستحسن — §3.2: تحديث «الاستخدام الحالي»

**الحالي:** «الاستخدام الحالي في الستاك: `10` بس (توصيل عادي)».
**بقى:** `10` (S1) · `25` و`30` (S2، من v6.0.0).

### 3.5 · 🔴 كاسر — §7: تسجيلات ناقصة

ده **مش تعديل قاعدة، ده بيانات ناقصة** — و`worker-builder` Rule 7 بتقول
التسجيل **قبل** أول `writeLog`.

**① صف كامل ناقص:**
```
| Bosta Orders Upload | `bosta_orders_upload` | `uploaded` · `upload_failed` ·
  `shopify_write_failed` · `skipped` · `login` · `logout` |
```
> البند ده مفتوح من قبل الجلسة دي (بند ١٨ في §11 · مسائل مفتوحة في
> `Bosta-Orders-Upload/CLAUDE.md`). الأداة **ما اشتغلتش حيًا** لسه، فلسه
> مافيش صفوف يتيمة — بس التسجيل لازم يسبق أول تشغيل.

**② صف `bosta_exchange_export` ناقصه ٦ قيم:**

الحالي:
```
`scan` · `export` · `export_return` · `export_exchange` · `confirm_return` ·
`confirm_exchange` · `manual_confirm_return` · `manual_confirm_exchange` · `login`
```
يزوّد عليه:
```
`cycle_block`                ← v5.4.0، مفتوح من 02-09-2026
`upload_re_return`           ← v6.0.0
`upload_re_exchange`         ← v6.0.0
`re_upload_failed`           ← v6.0.0
`re_shopify_write_failed`    ← v6.0.0
`re_cancelled`               ← v6.0.0
```

⚠️ **`re_upload_failed` و`re_shopify_write_failed` منفصلتين عمدًا** — الأولى
معناها مفيش شحنة اتعملت (إعادة المحاولة آمنة)، والتانية معناها الشحنة **موجودة
بفلوس** والكتابة الرجعية هي اللي فشلت (إعادة المحاولة = شحنة تانية). نفس الفصل
اللي في `bosta_orders_upload`.

### 3.6 · بند CHANGELOG المقترح

```markdown
## v2.1.0 — 10-09-2026

> **٢ بنود 🔴 · ٣ 🟡.** المصدر: نفس الفحص — `skillsupdates20260910.md`.
>
> **ليه MINOR رغم البندين الكاسرين:** الأول (§7) **بيانات ناقصة مش قاعدة
> اتغيّرت** — مفيش أداة بتعمل حاجة غلط، فيه صفوف سجل مش هيتعرف مصدرها.
> والتاني (§3.3) **نطاقه أدوات ما اتكتبتش لسه**: `Bosta-Orders-Upload`
> بيبعت القيمة الصح ليه بالفعل. مفيش أداة منشورة محتاجة retrofit.

🔴 كاسر — §7: تسجيل `bosta_orders_upload` (صف كامل) + ٦ قيم `type` ناقصة
   على `bosta_exchange_export`
   يخص: Bosta Orders Upload · Bosta Return/Exchange Exporter
   المكان: §7

🔴 كاسر — §3.3: `uniqueBusinessReference = رقم الأوردر` محجوز لأداة رفع S1؛
   أي أداة تانية على نفس الأوردر لازم قيمة مختلفة
   يخص: أي أداة بترفع شحنة تانية لنفس الأوردر
   المكان: §3.3

🟡 مُستحسن — §3: `COD_MAX = 30000` و`COD_REFUND_MIN = -2000` (errorCode 3008)
   يخص: أي أداة بترفع شحنة بوسطة
   المكان: §3

🟡 مُستحسن — §3.1: تسجيل مواقع الاستلام الأربعة بالـ _id (البند كان مفتوح)
   يخص: أي أداة بتحدد businessLocationId

🟡 مُستحسن — §3.2: الاستخدام الحالي بقى 10 و25 و30
   يخص: لا شيء مباشر — توضيح نطاق
```

---

## 4 · `shopify-graphql-helper` — المقترح: **v2.1.0 → v2.2.0**

### 4.1 · 🔴 كاسر — فلتر `metafields.custom.KEY:*` **مابيفلترش**

**الحالي (قسم Metafield filters):** بيغطي `KEY:VALUE` والنقطة مقابل النقطتين
و`adminFilterable` — **ومافيهوش أي كلام عن الـ wildcard**.

**المقيس 10-09-2026:**
```graphql
orders(first: 5, query: "metafields.custom.bosta_order_type:*")
```
رجّع **أحدث ٥ أوردرات في المتجر**، كلهم `bosta_order_type = null`. يعني الفلتر
**اتجاهل بالكامل** — الاستعلام اشتغل على المتجر كله من غير أي خطأ.

**الأثر:** ده فشل صامت من نوع «رجّعت نتايج، والنتايج غلط» — أخطر من صفر نتيجة.
أي سؤال من نوع «أنهي أوردرات عليها الحقل ده؟» بيرجّع إجابة تبان معقولة وهي
إجابة عن سؤال تاني خالص.

**الصح:** مفيش صيغة wildcard مضمونة للميتافيلد هنا. لو محتاج «الحقل مش فاضي»:
اسحب الحقل واعمل الفلترة **client-side**، أو استخدم قيم معروفة صراحة
(`KEY:A OR KEY:B`).

> **يخص:** أي أداة أو داشبورد بيسأل «مين عليه الحقل ده؟»
> **المكان:** Step 3 (Metafield filters) + سطر في Quick Pitfall Checklist.

### 4.2 · 🟡 مُستحسن — `shippingAddress`: الحقول الإلزامية لأي رفع شحن

`name` لوحده مش كافي. أي أداة بترفع على بوسطة محتاجة تسحب:
```graphql
shippingAddress { name firstName lastName phone address1 address2
                  city province provinceCode zip }
```
- `firstName` — **الحقل الوحيد الإلزامي** للاسم عند بوسطة.
- `provinceCode` — المفتاح الأساسي لجدول المحافظات (الاسم fallback بس).
- امتلاء مقيس على ٥٠ أوردر R/E: `firstName` **50/50** · `provinceCode` **50/50**.

🔴 **والتليفون تلات أشكال في المتجر** — `01…` · `+201…` · **وبمسافات**
(`+20 12 71043044` على `#53849`). التطبيع إلزامي **قبل الإرسال** مش للمقارنة بس.

### 4.3 · بند CHANGELOG المقترح

```markdown
## v2.2.0 — 10-09-2026

> **بند 🔴 · بند 🟡.** المصدر: نفس الفحص — `skillsupdates20260910.md`.
>
> **ليه MINOR رغم البند الكاسر (نفس منطق v2.1.0):** الـ wildcard **ماكانش
> موثّق في المهارة أصلًا** — مفيش أداة نسخته من هنا. البند إضافة فخ جديد،
> مش قلب قاعدة. بيتصنّف 🔴 لأن أي أداة وقعت فيه بتديّ **نتايج غلط تبان
> معقولة**، فالجرد إلزامي.

🔴 كاسر — `metafields.custom.KEY:*` بيتجاهل الفلتر بالكامل ويرجّع المتجر كله
   من غير خطأ — فشل صامت بنتايج غلط، مش بصفر نتيجة
   يخص: أي أداة أو داشبورد بيفلتر على وجود ميتافيلد
   المكان: Step 3 (Metafield filters) · Quick Pitfall Checklist

🟡 مُستحسن — الحقول الإلزامية في `shippingAddress` لأي أداة شحن
   (firstName · provinceCode) + التليفون بتلات أشكال بيحتاج تطبيع قبل الإرسال
   يخص: أي أداة بترفع شحنة لأي كوريَر
```

---

## 5 · `ecommoda-order-lifecycle` — المقترح: **v1.5.0 → v1.6.0**

### 5.1 · 🟡 مُستحسن — `state-machines.md`: `In-Return` بقى ليه كاتب تاني

**الحالي:** «Set automatically by **Bosta only**» — والاستثناء الموثّق إن
`Bosta-Return-Exchange-Exporter` بتكتبها يدوي عند تأكيد الموظف.

**بقى:** من v6.0.0، اللي بيكتبها هو **نجاح إنشاء الشحنة على بوسطة بالـ API** —
مش ضغطة تأكيد يدوية. الاستثناء لسه قايم (مفيش auto-sync من بوسطة)، بس
**المُحفّز اتغيّر** من «الموظف قال إنه رفع» لـ«بوسطة ردّت برقم تتبع».

⚠️ فرق حقيقي: القديم كان بيعتمد على كلام الموظف، الجديد على رد النظام الخارجي.

### 5.2 · 🟡 مُستحسن — Rule 16: قاعدة تشغيلية جديدة عن كوريَر المرتجع

**قرار أحمد 10-09-2026:** المرتجع **دايمًا** بيروح بنفس كوريَر الشحنة الأصلية.

عشان كده أداة S2 بتفلتر على `custom.courier = 'Bosta'` (الكوريَر اللي شحن
**الأصل**) مش على `custom.zone` (قناة التوجيه). ده مش تناقض مع Rule 16 — ده
سؤال تاني: Rule 16 بتجاوب «مين هيشحن؟»، والفلتر ده بيجاوب «مين استلم قبل كده؟».

قرينة داعمة (٥٠ أوردر R/E): كل `courier = Bosta` عليه `zone = Other_Regions`
والعكس — صفر استثناء.

> **يخص:** أي أداة بتختار أوردرات مرتجعات لكوريَر معيّن.

### 5.3 · ⚪ تحريري — بند مرحّل من `skillsupdates20260909.md`

الملف ده **اتطبّق بالفعل** (order-lifecycle بقى v1.5.0 وgraphql-helper v2.1.0).
مفيش بند متبقي منه — يتشال من «مسائل مفتوحة» في
`Bosta-Return-Exchange-Exporter/CLAUDE.md` وقت ما البصمة تتحدّث.

### 5.4 · بند CHANGELOG المقترح

```markdown
## v1.6.0 — 10-09-2026

> **بندين 🟡.** المصدر: نفس الفحص — `skillsupdates20260910.md`.
> مفيش بند كاسر: القواعد ما اتقلبتش، اتضاف عليها سياق جديد.

🟡 مُستحسن — `In-Return` في S2: المُحفّز بقى رد بوسطة برقم تتبع، مش تأكيد
   يدوي من الموظف
   يخص: Bosta Return/Exchange Exporter · أي قراءة لتوقيت In-Return
   المكان: references/state-machines.md

🟡 مُستحسن — المرتجع بيروح بنفس كوريَر الشحنة الأصلية (قرار أحمد) — عشان كده
   فلتر أدوات المرتجعات على `custom.courier` مش على `custom.zone`
   يخص: أي أداة بتختار أوردرات مرتجعات لكوريَر
   المكان: Rule 16
```

---

## 6 · `ecommoda-worker-builder` — المقترح: **v3.0.0 → v3.1.0**

> البندين دول **مرشحين، مش تصحيحات**. الاتنين أنماط طلعت من بناء v6.0.0
> ومسكتهم مراجعة كود، ومش لاقيهم في المهارة. لو شايفهم مغطيين بالفعل، اتجاهلهم.

### 6.1 · 🟡 مُستحسن — حارس بيشتغل على استعلام مختصر لازم يتعاد على القراءة الكاملة

النمط: أداة بتفحص أهلية على استعلام **رخيص** (scalars بس)، وبعدين بتقرا
البيانات الكاملة، وبعدين بتنفّذ فعل لا رجعة فيه. بين الفحص والتنفيذ فيه نافذة
الحالة ممكن تتغيّر فيها.

**القاعدة:** لو القراءة الكاملة بتحسب نفس المعلومة أصلًا (وهي بتحسبها غالبًا)،
أعِد الفحص عليها قبل الفعل. التكلفة **صفر** والنافذة بتتقفل.

### 6.2 · 🟡 مُستحسن — علم تحقق واحد للدفعة ممنوع يوسم نتايج فردية

مسكتها مراجعة كود في v6.0.0: `verifyManualStatus` بترجّع **قايمة عدم تطابق لكل
أوردر**، والكود كان بيحوّلها لـ`boolean` واحد للدفعة. النتيجة: أوردر واحد يفشل
تحققه → **كل** الصفوف بتتوسم فشل كتابة، وبتتسجّل بالحالة القديمة، وصفوف
`metafields_change` بتاعة اللي نجحوا فعلًا **مابتتكتبش** — يعني تحديث سليم
بيختفي من KPIs زمن الدورة.

**القاعدة:** التحقق اللي بيرجّع نتيجة لكل كيان، النتيجة بتتطبّق **لكل كيان**.
الاستثناء الوحيد: الكتابة نفسها رمت استثناء — ساعتها مش عارفين مين وصل، فالكل
بيتعامل كغير متحقَّق.

⚠️ البند ده بيوصل لـ`extra.result` و`metafields_change` مع بعض — فالخطأ فيه
بيبوّظ عدّادين في نفس الوقت.

### 6.3 · بند CHANGELOG المقترح

```markdown
## v3.1.0 — 10-09-2026

> **بندين 🟡 — مرشحين.** الاتنين أنماط طلعت من بناء
> `Bosta-Return-Exchange-Exporter` v6.0.0 ومسكتهم مراجعة كود.
> لو مغطيين بالفعل في v3.0.0، البند يتلغي والإصدار مايتحركش.

🟡 مُستحسن — حارس اشتغل على استعلام مختصر يتعاد على القراءة الكاملة قبل أي
   فعل لا رجعة فيه
   يخص: أي Worker بيفحص أهلية ثم يقرا ثم ينفّذ
   المكان: Step 5A ⑩

🟡 مُستحسن — تحقق بيرجّع نتيجة لكل كيان يتطبّق لكل كيان، مش كعلم واحد للدفعة
   يخص: أي Worker بيكتب على دفعة ويتحقق منها
   المكان: Step 5A ② / ④
```

---

## 7 · جدول النطاق — مين متأثر بأنهي بند 🔴

| البند | الأدوات المتأثرة | حالتها |
|---|---|---|
| `type.value` / الفلترة بالكود | أي أداة بتفلتر شحنات بوسطة — **مرشحين للفحص:** `bosta-order-lookup-worker` · `order-printer-worker` · سكانرات الحالة | 🔴 **محتاج جرد** |
| `uniqueBusinessReference` | `Bosta-Orders-Upload` (لسه صح — القيمة بتاعته) · `Bosta-Return-Exchange-Exporter` v6.0.0 (متطبّق) | ✅ الاتنين متسقين |
| Step 8b (عقد CRP/Exchange) | `Bosta-Return-Exchange-Exporter` v6.0.0 بس | ✅ متطبّق |
| §7 التسجيلات | `Bosta-Orders-Upload` · `Bosta-Return-Exchange-Exporter` | 🔴 **حاجز على أول تشغيل حي** |
| `metafields.custom.KEY:*` | أي داشبورد أو تقرير بيسأل «مين عليه الحقل ده» | 🔴 **محتاج جرد** |

**أمر الجرد المقترح للبند الأول والأخير:**
```bash
grep -rn "type?.value\|type\.value ===\|'EXCHANGE'\|'CUSTOMER_RETURN_PICKUP'" --include="*.js" --include="*.html" .
grep -rn "metafields\.custom\.[a-z_]*:\*" --include="*.js" --include="*.html" .
```

---

## 8 · اللي **مش** في الملف ده عن قصد

- **ب-٨** (هل رسم فتح الطرد 7ج بيتحسب على شحنة استرجاع؟) — لسه مش مقيس،
  محتاج مراجعة فاتورة بوسطة. مايتكتبش في مهارة قبل ما يتأكد.
- **قيم `type` في `/deliveries/search`** — البند 2.1 موقوف على نداء تأكيد واحد.
- **`ecommoda-html-builder`** — مفيش بند. اللي اتعمل في واجهة v6.0.0 (أربع
  حالات · تأكيد قبل فعل لا رجعة فيه · قفل المدخلات أثناء التنفيذ) كله مغطى
  بالفعل في v7.0.0.
- **`ecommoda-debugger`** — عيلة الفشل الكاذب مغطاة. الحالة الجديدة الوحيدة
  (500 بلا `errorCode`) مكانها الطبيعي `bosta-api-helper` مش هنا.

---

## 9 · Checklist التطبيق

```
[ ] نداء تأكيد واحد: POST /deliveries/search على #53849 → شكل type
[ ] bosta-api-helper      v1.1.0 → v2.0.0   (٣ 🔴 · ٤ 🟡 · ١ ⚪)
[ ] ecommoda-constants    v2.0.0 → v2.1.0   (٢ 🔴 · ٣ 🟡)
[ ] shopify-graphql-helper v2.1.0 → v2.2.0  (١ 🔴 · ١ 🟡)
[ ] ecommoda-order-lifecycle v1.5.0 → v1.6.0 (٢ 🟡)
[ ] ecommoda-worker-builder  v3.0.0 → v3.1.0 (٢ 🟡 — مرشحين)
[ ] جرد `type.value` على الـ ٢٨ أداة
[ ] جرد `metafields.custom.KEY:*` على الـ ٢٨ أداة
[ ] بعد كل ده: skills-sweep على الريبوين وتحديث البصمة
```

⚠️ **البصمة في الريبوين متسابة زي ما هي عن قصد** لحد ما التطبيق ده يخلص —
تحديثها دلوقتي معناه ادّعاء مراجعة امتثال ما حصلتش.

</div>
